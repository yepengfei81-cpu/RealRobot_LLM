# AIRBOT gRPC



## Getting started

Install required dependency packages first:

```bash
pip install -r requirements.txt --break-system-packages -i https://pypi.tuna.tsinghua.edu.cn/simple
```

```bash
bash ./generate_python.sh
```

The gRPC files will be generated in the `airbot_grpc` folder in the current directory.
